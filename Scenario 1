<?php
$conn = mysqli_connect("localhost", "root", "", "class_db");
// Wrong: Using $_POST instead of $_GET for URL parameter
// $id = $_POST['id'];
$id = isset($_GET['id']) ? intval($_GET['id']) : 0;
if ($id > 0) {
    $stmt = mysqli_prepare($conn, "SELECT * FROM students WHERE student_id = ?");
    mysqli_stmt_bind_param($stmt, "i", $id);
    mysqli_stmt_execute($stmt);
    $res = mysqli_stmt_get_result($stmt);
    $r = mysqli_fetch_assoc($res);
    echo $r['first_name'];
    mysqli_stmt_close($stmt);
}
mysqli_close($conn);
?>
/*
Ay mali yan kasi ang URL parameter na ?id=3 ay dapat gamitin ang $_GET, hindi $_POST. Ginawa ko na correct para di mag-error ang "Undefined index". Dagdag pa, ginamit ko prepared statements para secure at validation para safe.
*/
